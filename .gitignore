# See http://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
node_modules

# testing
coverage

# production
build

# misc
.DS_Store
.env
# Qualquer variante (.env.local, cópias de segurança) — nunca versionar segredos
.env.*
!.env-template
npm-debug.log
yarn-error.log

# Runtime counters / stats — written by the API server, not part of source.
server/data/listing-views.json
server/data/pwa-counter.json
server/data/waitlist.json
